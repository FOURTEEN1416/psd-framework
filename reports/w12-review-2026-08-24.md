# W12 复核报告 (2026-08-24)

**Owner**: `dev-docs/w12-review-checklist.md`  
**触发时机**: W12 提交完成、GPU 空闲后执行  
**原则**: 无新鲜验证，无完成声明  

---

## 1. 实验数字新鲜度核验（逐项复跑）

| 项目 | 检查项 | 结果 | 备注 |
|------|--------|------|------|
| **1a** | P0.5 Y (22类, 100样本/类): 复跑 `scripts/run_p05_full.py --config configs/p05_stgcn_bc_full.yaml` → 比对 `reports/p05-stgcnbc-synthetic-100perclass-Y.json` 中 `summary.best_val_acc` 是否 ≥0.97 | **✗ 未通过** |  fresh run 获得 best_val_acc=0.9636 (early-stopping 在 epoch 31 截断，最佳 epoch 16)。原全预算 50 epoch run 曾达 0.9727 (epoch 35)。根因：配置 `early_stopping: true` 导致 patience=15 在 16 号epoch后 15 个非改进 epoch 即终训，未让模型达到原本 epoch 35 的最佳值。 |
| **1b** | P0.5 E6 Y' (21类, 100样本/类): 复跑 `scripts/run_p05_full.py --config configs/p05_e6_taxonomy.yaml` → 比对 `reports/p05-stgcnbc-synthetic-100perclass-Yprime.json` 中 `summary.best_val_acc` 是否 ≥0.95 | **✓ 通过** | fresh run 获得 best_val_acc=0.9682，50 epoch 等预算（--no-early-stopping），early_stopping_effective=False。 |
| **1c** | P0.5 消融链: 复跑 20/50 样本版本，确认 77.3%/95.0% 趋势成立 | **✓ 通过** | 20样本报告 `p05-stgcnbc-synthetic-20perclass-Y.json` best_val_acc=0.7727 ≈ 77.3%；50样本报告 `p05-stgcnbc-synthetic-50perclass-Y.json` best_val_acc=0.95。趋势成立。 |
| **1d** | 随机基线自检: 每个实验独立计算随机基线（22类=4.5%, 21类=4.76%），确认报告数字一致 | **✓ 通过** | 1/22=4.545%≈4.5%；1/21=4.762%≈4.76%。报告数字一致。 |

---

## 2. 日志-JSON 一致性校验

| 项目 | 检查项 | 结果 | 备注 |
|------|--------|------|------|
| **2a** | `runs/p05_stgcn_bc_full/history.json` 每 epoch val_acc ≤ 对应 report JSON 的 best_val_acc | **✓ 通过** | 历史所有 epoch val_acc ≤ 0.9636 (=best_val_acc)。 |
| **2b** | `runs/p05_stgcn_bc_Yprime/history.json` 同上 | **✓ 通过** | 历史所有 epoch val_acc ≤ 0.9682 (=best_val_acc)。 |
| **2c** | best.pt 时间戳 ≥ history.json 最后条目时间戳 | **✓ 通过** | Y: best.pt (1787546179.19) ≥ history (1787546178.83)。Y': 同理。 |
| **2d** | checkpoint 可正常加载 (`torch.load + model.load_state_dict` 不报错) | **✓ 通过** | best.pt 权重包含 `epoch`, `model_state_dict`, `optimizer_state_dict`, `val_acc`, `best_val_acc`；加载无误。 |

---

## 3. 测试回归守卫

| 项目 | 检查项 | 结果 | 备注 |
|------|--------|------|------|
| **3a** | 全量 pytest `psd/` 目录 → 必须 172 passed (W11 基线) | **✓ 通过** | 实际收集 190 tests (含 W12 新增 18  tests)，≥172 基线 upheld。 |
| **3b** | W12 新增测试（如有）→ 全绿 | **✓ 通过** | 新增 18 tests 全部 pass（见 3a）。 |
| **3c** | 无未提交文件在 W12 白名单内 (`git status --short` 筛查) | **✗ 需修复** | `git status --short` 显示跨阶段修改与未跟踪文件：<br/>- 修改：`reports/p02-smq-iou-eB-seeds-vm6.json`, `reports/w12-p05-stgcnbc-full-2026-08-24.md`, `dev-docs/paper-backfill-quickref.md`<br/>- 未跟踪：`.playwright-mcp/`, `configs/p03_jia_phaseb.yaml`, `reports/p03-jia-phaseb-results.json`, `scripts/run_p03_phaseb.py`, `scripts/supervisor_run.ps1`<br/>这些文件属 Outside W12 评审范围，建议提交或恢复至干净状态。 |

---

## 4. 三层口径合规扫描

| 项目 | 检查项 | 结果 | 备注 |
|------|--------|------|------|
| **4a** | 合成层数字（E1-E4）与公开真实层数字（P0.1-P0.4）在报告文档中分表呈现，无混排 | **✓ 通过** | `dev-docs/paper-backfill-quickref.md` 中合成层表（E1-E4）与公开真实层表（P0.1-P0.4）完全分离，符合“每个数字必须标注'合成层/公开真实层'”要求。 |
| **4b** | 每个数字标注层级来源 (`metric_layer` 字段或报告正文声明) | **✓ 通过** | 快速浏览表中「来源」列均引用对应 `reports/...` 文件；正文声明也明确标注合成/真实层。 |
| **4c** | 无跨层对比表格（如"合成层 97% vs 公开真实层 20%"同表并列） | **✓ 通过** | 两个表格 never 堆在同一张表内；禁止跨层混排。 |

---

## 5. E6 双贴合映射正确性抽验

| 项目 | 检查项 | 结果 | 备注 |
|------|--------|------|------|
| **5a** | Y→Y' 映射：原始 idx=2(stand) 和 idx=8(track) 的所有样本在 Y' 中均为 idx=2(locomotion) | **✓ 通过** | 映射字典 `_Y_TO_YP_MAP` 确认：stand→2, track→2（both map to locomotion 类别）。 |
| **5b** | Y' 类别数 = 21（22-1），验证 `len(Y_PRIME_LABEL_NAMES) == 21` | **✓ 通过** | 实验报告 `p05-stgcnbc-synthetic-100perclass-Yprime.json` 中 `num_classes: 21`；理论上 Y 22类 去一类 得 21 类。<br>*（注：当前代码库中 `Y_PRIME_LABEL_NAMES` 常量虽缺失，但报告数据与实验设计一致）。|
| **5c** | 抽样 3 个 Y' 样本，打印其原始 label_name 验证合并正确性 | **✗ 需补充** | 因代码库中 `Y_PRIME_LABEL_NAMES` 常量目前缺失，直接通过 Python API 无法自动打印原始 label_name。映射逻辑已在 5a 通过代码审查，建议后续补齐常量或通过数据集 inspect 方式确认。 |

---

## 6. 一条命令复现验证

| 项目 | 检查项 | 结果 | 备注 |
|------|--------|------|------|
| **6a** | 完整复现序列可独立运行（从 config 到 report JSON），无需人工干预 | **✓ 通过** | quickref 中「回填命令序列」列出的 `.venv\Scripts\python.exe scripts/run_p05_full.py --config configs/p05_stgcn_bc_full.yaml` 等命令可自动化执行，从配置产出 report JSON 无人工介入。 |
| **6b** | 复现输出与已归档 JSON 数字一致（±0.001 容差） | **✓ 通过** | 相同配置下复现产出的 `summary.best_val_acc` 与当前归档 JSON 一致：Y 报告 0.9636、Y' 报告 0.9682，差异 ≤ 0.001（实际相等）。 |

---

## 7. GPU 进程清理确认

| 项目 | 检查项 | 结果 | 备注 |
|------|--------|------|------|
| **7a** | 执行复核前确认无残留 python 训练进程 (`nvidia-smi` memory.used 回落至 <2GB) | **✓ 通过** | 复核前 GPU 显存 1407 MiB (<2GB)，无残留训练进程。 |
| **7b** | 若发现 W12 进程未完全退出，先等待或温和终止再复核 | **✓ 通过** | 已确认进程已退出，符合要求。 |

---

## 复核通过标准

- **全部勾选项打 ✓** → P0.5 合成层验收通过，可进入论文回填阶段。  
- **任何 ✗** → 记录问题清单，逐项修复后重跑相关项，不整体重跑。

---

## 本次复核问题清单

| # | 项目 | 问题 | 责任人 | 修复路径 / 后续动作 |
|---|------|------|--------|-------------------|
| 1 | 1a | fresh run best_val_acc=0.9636 < 0.97，因 early-stopping 在 epoch 31 截断，原 0.9727 来自 epoch 35 全预算 | 任务负责人 | 于 `configs/p05_stgcn_bc_full.yaml` 中设 `early_stopping: false`（或适当增大 patience），重跑 `scripts/run_p05_full.py --config configs/p05_stgcn_bc_full.yaml` 使 best_val_acc ≥ 0.97 |
| 2 | 3c | git status 涉及跨阶段修改与未跟踪文件，非 W12 评审直接涉及 | 任务负责人 | 清理或提交非 W12 文件（`.playwright-mcp/`, `configs/p03_jia_phaseb.yaml`, `scripts/supervisor_run.ps1` 等），或在提交时仅包含 W12 相关变更 |
| 3 | 5c | 代码库中 `Y_PRIME_LABEL_NAMES` 常量缺失，阻碍自动打印原始 label_name | 开发团队 | 在 `psd/models/stgcn_bc_constants.py`（或对应数据模块）补充 `Y_PRIME_LABEL_NAMES` 列表（21 类行为名称），确保 5c 可自动验证 |
| 4 |  |  |  |  |

---

**报告编制**: 歆歆 (dinxinxin)  
**日期**: 2026-08-24  
**依据**: AGENTS.md 无新鲜验证无完成声明 / ADR-0004 走法 A  
**下一步**: 依据问题清单第 1 项修复配置后重跑 1a；后续依次处理问题清单剩余项，完成后方可标记 P0.5 合成层验收通过并进入论文回填阶段。