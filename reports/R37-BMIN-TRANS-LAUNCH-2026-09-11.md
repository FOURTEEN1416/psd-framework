# R37 B-MIN-TRANS 启动记录（2026-09-11）— 状态：管线已建成并跑通，格式破译完成，校准迭代进行中

## 1. "是不是已经跑过"——查证结论（用户直觉部分正确）

- **NTU 60→49 迁移 + 三臂结构确实跑过**：TRANS-002（`run_r23_trans002.py`）C/M/D′ 三臂齐全，工件含 `D_prime_reused`（D′ 三种子 764/626/172s, val 75.33%）——既视感来源 ①；
- **规则种子管线整套存在但全是犬类域**：`make_rule_seeds.py`（W6 七类规则族）/`harvest_rule_seeds.py`（W25 片段池）/`calibrate_rule_seeds_2d.py`——既视感来源 ②；
- **B-MIN-TRANS 的 D 臂（NTU 上仅规则种子的解耦头）从未跑过**：TRANS-002 的 D′ 消费的是 Y′ 合并映射标签（有监督），非规则种子。结论：实验是新的，但 C/M 臂数值可直接复用 TRANS-002（79.8%/82.6%），新算力 = CPU 分钟级。

## 2. 已建成（scripts/make_rule_seeds_ntu_bmin.py，端到端 5 秒跑通）

- 特征层：train_position.npy (40091,3,50,25,2) memmap 分块 → 14 维运动学特征；
- 规则层：49 类 Y′ 规则表（公开类名语义+几何先验；8 个双人交互类按设计留 unknown）；
- 校准层：种子精度 vs train Y′ GT（事后诊断）+ 门（精度 ≥0.5 且覆盖 ≥0.3）；
- D 臂：StandardScaler+LogisticRegression（E9 求解器）仅喂种子标签 → val 49 类 top-1 vs TRANS-002 C 臂 79.8%。

## 3. 校准迭代日志（两轮，均为格式问题而非科学结果）

- 第 1 轮：竖轴判到 x 轴 → 精度 2.19%（≈随机 1/49）；
- 第 2 轮：竖轴修正为 y 向下取负（P(head>base)=0.13 实测）→ 精度 1.76%，仍 ≈随机；
- **第 3 轮前破译（关键）**：该 pack 是 AimCLR 标准 (C=3, T, V=25, M=2) 布局——**C=3 是 (x,y,z) 三维坐标、M=2 是双人体**。此前只消费了 x/y 两维+单人体，且 size 归一化用了 2D 距离。 posture/高度特征必须用 3D（y 上向需再验），双人类（strike/hug/handshake 等 8 类）可用 M=1 体做规则——这不是死局，是特征层待重写。

## 4. 下一步（下一轮继续）
1. 特征层重写为 3D 双人版：size=|neck−base| 3D；posture 用 y（上向再验：sit/jump 类对照）；z 做前后向特征；M=1 有值时启用双人规则；
2. 重跑校准 → 门过则 D 臂训练出判定，不过则按预注册条款记 FAILS（此时才是真结果）；
3. 判定写入 reports/ + 论文增补（camera-ready 弹药）。

## 5. 纪律声明
- 本轮两轮低精度均系**格式解读错误**（竖轴反/丢 z/丢第二人体），按预注册条款不得记为 B-MIN-TRANS 实验结论；
- 规则阈值先验固定未调参的纪律保持；标签仅用于事后诊断精度，未进规则构建。
