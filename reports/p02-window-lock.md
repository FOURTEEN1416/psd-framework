# P0.2 救援窗口锁（2026-08-24 01:50）

> **本文件是窗口协调标记，不是 truth 文档。** 归档于 `reports/p02-*` 白名单内。

## 当前独占窗口

- **W7 救援执行权归持有本锁的会话**（2026-08-24 01:45 接管，用户指令「接手该项目」）。
- 若你是另一个被继续使用的 W7/P0.2 会话：**请立即停手待命，不要启动任何 GPU 实验、不要提交 p02 领地文件、不要撰写 p02 报告**。共享记忆 vault 有相同声明（搜索 `psd-framework W7`）。

## 正在进行的工作（勿重复执行）

| 项 | 状态 |
|----|------|
| E-A 单变量实验（mse_loss_weight 0.001→1.0） | 🔄 运行中至 ~05:00，孤儿进程 PID 19080/9968，输出 `runs/p02_smq_eA/` |
| E-A 后双口径评估（concat + seeds） | ⏳ 待训练完成，由持锁窗口执行 |
| Step A-2 种子伪 GT sanity check | ⏳ 排队中 |

## 他窗安全须知

- E-A 是孤儿进程：关闭任意 opencode 窗口**不会**中断训练。
- GPU 显存仅剩 ~0.5GB：**在他窗启动任何 CUDA 任务都会 OOM**。
- git 工作树应保持干净；如你看到未提交的 p02 文件且非你所写，先 `git log -3 --oneline` 查归属再决定。
