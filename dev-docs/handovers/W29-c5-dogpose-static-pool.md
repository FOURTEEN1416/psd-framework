# W29 任务书 — C5 dog-pose 静态池评估（已完成 · 三项决策升级待主窗口）

> 状态: ✅ 评估线完成（2026-08-25，wt/W29 @ `2e324e3`）；⚠️ 发现公开真实层系统性问题，**D1-D3 待主窗口统一裁决**
> 结论速览: dog-pose 取 **(b) 仅作预训练/增广池**；完整证据 `reports/dogpose-report-2026-08-25.md`
> 本册性质: 跨窗升级单——问题超出 C5 领地，处置动作涉及他窗文件，只报不修

---

## 0. 给主窗口的 30 秒版

dog-pose 数据本体零缺陷、无序列元数据（结论 b 成立已入库）。但盘点揪出一个**波及整个公开真实层的系统性缺陷**：

> **GT 有效关节只有 20/24**——left_eye / right_eye / **withers(K9Graph 根关节)** / throat 在全部 8476 图可见率 0%。
> p05 报告 §4.1 的"24 点原生=K9Graph 零投影（含 withers/throat）"仅在**拓扑命名层**成立；其 GT 标注层这 4 关节从未被标注过。

传染链：dog-pose GT 缺失 → p05 微调的 YOLO 权重 4 通道无监督信号（输出不可信且**置信度过滤不可靠**）→ Q3a/C1/W20 全量提点继承垃圾通道 → 冻结骨干 warm-start 微调中，根关节噪声经图卷积扩散污染全身特征 → 规则引擎依赖 withers/throat 的规则族在真实域无法计算。

不受影响：合成层全部数字、恒等映射代码、ST-GCN 架构维度（格式第三维天然支持表达缺失）。

## 1. 证据指针（主窗口可直接抽查）

| 证据 | 位置 |
|----|----|
| 全量盘点 JSON（配对/解析/逐关节可见率/序列探查） | `runs/data_campaign/dogpose/inventory-evidence-2026-08-25.json` |
| 完整评估报告（三选一双向论证/AK 对比/W26 合流草案） | `reports/dogpose-report-2026-08-25.md` |
| 入库产物 + manifest（sha256 固化，synthetic_dynamic=false） | `runs/data_campaign/dogpose/sequences/*.pkl` + `manifest-2026-08-25.json` |
| 复现/复核命令 | 报告 §4（ingest + verify 两脚本，当次运行退出码 0） |
| 提交 | wt/W29 `2e324e3`（5 文件白名单，pytest 296 绿） |

## 2. 待裁决菜单（D1-D3）

### D1 公开真实层 4 关节硬掩码补丁 【建议：立即】
- **内容**: `psd/data/ak_pose_extract.py` 组装环节对索引 {20,21,22,23} 强制 v=0 + fail-fast 断言（仿既有 24 点防呆）；GPU 接力队列 Q3c 的提点步骤②③**必须在此补丁落地后再跑**，否则垃圾通道进 warm-start 微调。
- **归属**: W20/p05 领地，C5 不越界 → 需派单（新窗口或并入现有关键窗口）。
- **代价**: 半小时内的小补丁 + 测试；**不做的风险**: 公开真实层数字被静默毒化，返工成本大。

### D2 StanfordExtra 原始标注核查（根治路线）【建议：短期调研】
- **假设（待核实，勿直接引用）**: StanfordExtra 原始 24 点方案本应含双眼/withers/throat，疑为 ultralytics 打包转换丢失。
- **动作**: GitHub-First 调研转换脚本与原始标注；若属实→回原始标注重建训练集、重训 YOLO（~1h GPU，排 W18 队列）→ 根治。
- **代价**: 半天调研 + 1h GPU；不做则永久带 20/24 口径运行。

### D3 论文口径措辞修正 【建议：随收编立即落】
- **内容**: 凡"24 点原生=K9Graph 零投影（含 withers/throat）"表述处，加 GT 层限定："拓扑同构，有效监督关节 20/24"，limitations 一句带过。
- **涉及**: p05 报告（他窗 truth，需协调层授权修订或批注）、paper-backfill 相关文档。
- **代价**: 一句话；不做的风险: 审稿人查数据卡即戳穿。

### 附带事项（无需决策，登记备查）
- W26 产出当时不可读（未开工），静态资产合流草案在评估报告 §6，待 C2 收编后合流。
- HANDOVER.md §8 路由行与状态行更新：留给本册收编时由协调层顺手执行（避免并行窗冲突）。

## 3. 收编与卸窗

```
# 主检出执行：
git merge --no-ff wt/W29
# 确认收编后清理：
pwsh scripts/new_window_worktree.ps1 W29 -Remove
```

## 4. 三层口径声明

本次所有数字属公开真实层·静态子池；发现的问题同样仅影响公开真实层及其下游，合成层与真实 K9 层无涉。
