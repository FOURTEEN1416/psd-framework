% 05-ablation-analysis.tex — 装配源 truth: docs/paper/experiment-skeleton.md §5 (tab3 七行 + 低资源梯度叙事段 v1.1 + P0.8 语义warm-start消融) + fig4 (FIGURE_SOURCE.md v0.2 caption truth)
% 纪律: E5 探索性发现措辞按裁决①(效率主张禁用); tab3 表格 + 低资源梯度段(成对呈现纪律: 低资源档与饱和档必须成对出现);
% fig4 caption 逐字取 FIGURE_SOURCE.md; 四步分析结构 observe→interpret→implicate→next。
\section{Ablation and analysis}\label{sec:ablation}

Table~\ref{tab:ablation} summarizes the component-level ablations; each row removes, replaces, or perturbs one design element and reports the measured consequence under its disclosed caliber.

\begin{table}[t]
\centering
\caption{Component ablations. Every number is tier-tagged; tiers are never mixed. Paired designs are described inline. Device calibers are disclosed where a row spans devices (row 1: gradient tiers CPU, saturation tier GPU); cross-device absolute accuracies are not poolable (Section~\ref{sec:conclusion}, L10).}
\label{tab:ablation}
\small
\begin{tabularx}{\linewidth}{p{2.4cm}p{1.4cm}X}
\hline
Switch & Claim & Result (caliber) \\
\hline
$-$ self-supervised pretraining & C3 & Benefit decays to statistical equivalence with budget: $+21.2$\,pp at 88 clips (scratch collapses to $\approx$2.3$\times$ random), $+9.9$\,pp at 176, $-2.3$\,pp at 352 (crossover region, within one arm's std), $+0.15$\,pp n.s.\ at 1760 \emph{(synthetic; scratch vs.\ warm, 3 seeds, matched budgets; gradient tiers CPU, saturation tier GPU)} \\
$-$ semantic warm-start (generic SSL instead) & C6 & Under the corrected end-to-end protocol (final head on seed truth + pool pseudo-labels; 10 seeds at 2 and 4 clips/class; full-budget arm deterministic, not significance-tested), both arms are near chance at 2 clips/class (top-1 9.8 vs.\ 15.2; macro-F1 5.3 vs.\ 10.4, Holm-corrected $p=0.043$ favoring generic SSL) and ordered the same at 4 (10.9 vs.\ 15.9); at full supervision the supervised reference still favors the task-pretrained backbone (33.93 vs.\ 30.36). The earlier warm-start advantage at low budget was an artifact of a true-label head and is superseded \emph{(public-real; AimCLR@InterPet4D, mocap domain gap disclosed)} \\
$-$ unsupervised segmentation (replace SMQ) & C4 & SMQ 0.458$\pm$0.049 $>$ uniform equal-count window 0.399$\pm$0.035 $>$ random-cut null 0.323$\pm$0.022; all three pre-registered gates pass. A fixed-grid probe (0.453$\pm$0.027) is statistically on par with SMQ and disclosed as such; uniform windows lead on boundary F1 (0.396 vs.\ 0.343), reported to avoid single-metric cherry-picking \emph{(public-real; seeds pseudo-GT, 4 episodes)} \\
$-$ anchor guidance & C5 & Prototype purity drops from 0.5339 to a 0.3306 random-assignment baseline ($1.61\times$; all-majority baseline 0.4858); seed-ratio 25\%$\to$100\% adds $\leq$1\,pp at $K\in\{5,10\}$ but costs 2.4\,pp at the best sweep cell $K{=}14$ \emph{(public-real / P0.3)} \\
$-$ pseudo-label iteration (single round) & C5 & Pool precision 0.5125 $\to$ 0.6913 ($+17.9$\,pp at round 1; precision-drop stopping fired after two degraded rounds); an uncalibrated-prototype control fails to start iterating \emph{(public-real / P0.4, physics-prior consensus caliber)} \\
$-$ uncertainty sampling (use random) & C7 (exploratory) & No entropy advantage under cold-start weak scorers: random leads in mean by $+7.9$\,pp at budget 100 (2/3 seeds) and $+7.1$\,pp at 200 (3/3 seeds); direction confirmed with strong in-domain scorers ($+4.2$--$5.0$\,pp at every budget above the shared initial set) \emph{(synthetic; negative result reported as-is)} \\
seed-noise injection \{10/20/30\%\} & C5 & Purity 0.5339 $\to$ 0.5267 $\to$ 0.5201 $\to$ 0.5025 at 30\% ($-3.1$\,pp, still $1.52\times$ baseline)---iteration does not amplify seed noise at this scale \emph{(public-real / P0.3)} \\
\hline
\end{tabularx}
\end{table}

\paragraph{Pretraining value is a function of annotation scarcity.}
The pretraining row deserves its own reading because it quantifies the word \emph{low-resource} in our title. Across four matched budgets (88/176/352/1760 training clips, scratch vs.\ pretraining-initialized encoders, three seeds each, identical schedules), the fine-tuning benefit of the self-supervised encoder decays to statistical equivalence. It is $+21.2$\,pp at 88 clips---where training from scratch collapses to roughly $2.3\times$ the random baseline while the warm encoder reaches $7\times$ random---then $+9.9$\,pp at 176 clips (with warm-start std $3.8\times$ narrower, a reliability gain independent of the mean), $-2.3$\,pp at 352 clips (inside the theoretical crossover region, within one arm's standard deviation and disclosed as such), and $+0.15$\,pp (n.s.) at the saturated 1760-clip tier. The zero crossing lies between 10 and 20 clips per class; we bracket it rather than fabricate a precise location, and we pair the low-resource tiers with the saturated tier in every presentation to forestall cherry-picking. Together with the $k$-NN probe of Section~\ref{sec:main-results} (E1), this establishes two complementary lines of evidence for C3: representation discriminability (probe) and fine-tuning value (gradient). The mechanism reading is one sentence long: the scarcer the labels, the less a discriminative representation can be learned from data, and the cross-domain prior embedded by the pretraining-initialized encoder becomes the only usable structure.

\paragraph{Semantic warm-start versus generic SSL on real skeletons.}
The synthetic-tier row above asks whether pretraining helps at all; on the public-real tier we ask the sharper design question of \emph{which} pretraining earns the low-label budget. Holding the entire end-to-end pipeline fixed (seeds, anchor-guided pseudo-labeling, head, splits, leakage guards) and replacing the backbone initialization together with its bound native input preprocessing (each arm uses the preprocessing its method was trained with; disclosed)---task-pretrained semantic warm start versus a generic contrastive encoder pretrained on motion-capture dog skeletons (AimCLR)---the corrected pseudo-label protocol finds both arms near chance at 2 clips/class (top-1 9.8\% vs.\ 15.2\%; chance 11.1\%), with the generic SSL arm ahead on macro-F1 (10.4\% vs.\ 5.3\%, Holm-corrected $p=0.043$) and the same ordering at 4 clips/class; at full supervision the deterministic supervised reference still favors the task-pretrained backbone (33.93\% vs.\ 30.36\%). We disclose that the generic arm carries a mocap-to-in-the-wild domain gap that its original evaluation did not, so the arm is a transfer lower bound, not a best-tuned AimCLR. The reading this supports is a boundary condition rather than a mechanism: at a 13\% budget on this tier, neither initialization yields pseudo-labels good enough to train on, and the earlier warm-start advantage---reported under a protocol that trained the final head on pool ground truth---does not survive the correction. What the tier's supervised comparisons support (E4 pool precision; the five-arm study in Section~\ref{sec:comparison}) is that task-pretrained features are better when labels are present; whether pseudo-label quality tracks feature quality at budget is untested.

\paragraph{Uncertainty sampling: an exploratory negative finding (C7, downgraded by pre-registration).}
We report, as an exploratory finding, that uncertainty sampling did not help in our regime. Under cold-start weak scorers on the synthetic tier, entropy sampling showed no detectable advantage: random led in mean by 7.9\,pp at budget 100 (2 of 3 seeds) and 7.1\,pp at 200 (3 of 3 seeds). A full-budget rerun (120 vs.\ 50 epochs) reproduces the direction (12.3/7.8\,pp), and with warm-started in-domain fine-tuned scorers random still led by 4.2--5.0\,pp at every budget above the shared initial set ($b\geq 50$). A diagnostic explains the mechanism: the synthetic-trained checkpoint scored the public-real candidate pool with fully saturated softmax (mean top1$-$top2 logit margin 100.9, versus $\approx$10.8 within its own validation domain), reducing uncertainty signals to numerical zero. The classical preconditions of uncertainty sampling---a strong scorer and in-domain calibration---are exactly what a low-resource pipeline lacks at startup. This motivates a progressive annotation narrative (annotate first, calibrate the scorer, then select samples), which we leave to future work; we make no sampling-efficiency claims. Figure~\ref{fig:al-efficiency} shows both the cold-start and warm-started protocols.

\paragraph{Sensitivity.}
Seed-ratio sweeps show prototype quality saturating early ($\leq$1\,pp beyond 25\% seeds at $K\in\{5,10\}$; $-2.4$\,pp at the best sweep cell $K{=}14$); the confidence threshold $\tau$ and cluster count $K$ interact with the frequency-aware margin design, and full sweeps are registered as future work. The class-imbalance handling switch (frequency-aware margins vs.\ resampling) is registered for evaluation on the public-real tier (not yet run) together with the per-class breakdown mandated by Section~\ref{sec:conclusion}.

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{fig4_al_efficiency.pdf}
  \caption{Active-learning efficiency on the synthetic layer (short-budget protocol; best validation accuracy, 22-class top-1 on a fixed ground-truth set, $n=330$; mean $\pm$ std, 3 seeds). Panel (a) cold-start scorers; panel (b) warm-started in-domain scorers---the stronger negative evidence. Entropy sampling shows no detectable advantage: $b=20/50$ differences lie within noise, and random leads in mean at $b \geq 100$ (69.9 vs.\ 77.8\% at $b=100$, 2/3 seeds; 80.9 vs.\ 88.0\% at $b=200$, 3/3 seeds).}\label{fig:al-efficiency}
\end{figure}
