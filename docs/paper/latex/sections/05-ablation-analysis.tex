% 05-ablation-analysis.tex — 装配源 truth: docs/paper/experiment-skeleton.md §5 (tab3 七行 + 低资源梯度叙事段 v1.1 + P0.8 语义warm-start消融) + fig4 (FIGURE_SOURCE.md v0.2 caption truth)
% 纪律: E5 探索性发现措辞按裁决①(效率主张禁用); tab3 表格 + 低资源梯度段(成对呈现纪律: 低资源档与饱和档必须成对出现);
% fig4 caption 逐字取 FIGURE_SOURCE.md; 四步分析结构 observe→interpret→implicate→next。
\section{Ablation and analysis}\label{sec:ablation}

Table~\ref{tab:ablation} summarizes the component-level ablations; each row removes or replaces one design element and reports the measured consequence under its disclosed caliber.

\begin{table}[t]
\centering
\caption{Component ablations. Every number is tier-tagged; tiers are never mixed. Paired designs are described inline. Device calibers (CPU vs.\ GPU tiers) are disclosed per row; cross-device absolute accuracies are not poolable (Section~\ref{sec:conclusion}, L10).}
\label{tab:ablation}
\small
\begin{tabularx}{\linewidth}{p{2.4cm}p{1.4cm}X}
\hline
Switch & Claim & Result (caliber) \\
\hline
$-$ self-supervised pretraining & C3 & Benefit decays monotonically with budget: $+21.2$\,pp at 88 clips (scratch collapses to $\approx$2.3$\times$ random), $+9.9$\,pp at 176, $-2.3$\,pp at 352 (crossover region, below both arms' std), $+0.15$\,pp n.s.\ at 1760 \emph{(synthetic; scratch vs.\ warm, 3 seeds, matched budgets)} \\
$-$ semantic warm-start (generic SSL instead) & C6 & Under the identical end-to-end protocol (10 seeds at 2 and 4 clips/class; full-budget arm deterministic, untested), top-1 favors the task-pretrained warm start at every budget (2 clips/class: 31.96 vs.\ 26.07, all ten seed pairs, Wilcoxon $p=0.002$; 4: 31.43 vs.\ 24.11, $p=0.016$; full: 33.93 vs.\ 30.36); macro-F1 is at parity (14.36 vs.\ 14.78, $p=0.77$)---an $n=3$ reversal resolved as seed noise \emph{(public-real; AimCLR@InterPet4D, mocap domain gap disclosed)} \\
$-$ unsupervised segmentation (replace SMQ) & C4 & SMQ 0.458$\pm$0.049 $>$ uniform equal-count window 0.399$\pm$0.035 $>$ random-cut null 0.323$\pm$0.022; all three pre-registered gates pass. A fixed-grid probe (0.453$\pm$0.027) is statistically on par with SMQ and disclosed as such; uniform windows lead on boundary F1 (0.396 vs.\ 0.343), reported to avoid single-metric cherry-picking \emph{(public-real; seeds pseudo-GT, 4 episodes)} \\
$-$ anchor guidance & C5 & Prototype purity drops from 0.5339 to a 0.3306 random-assignment baseline ($1.62\times$); seed-ratio 25\%$\to$100\% adds $\leq$1\,pp \emph{(public-real / P0.3)} \\
$-$ pseudo-label iteration (single round) & C5 & Pool accuracy 0.5125 $\to$ 0.6913 ($+17.9$\,pp at round 1); an uncalibrated-prototype control fails to start iterating \emph{(public-real / P0.4, physics-prior consensus caliber)} \\
$-$ uncertainty sampling (use random) & C7 (exploratory) & No entropy advantage under cold-start weak scorers: random leads by $+7.9$\,pp at budget 100 and $+7.1$\,pp at 200 (3/3 seeds); direction confirmed with strong in-domain scorers ($+4.2$--$5.0$\,pp at all budgets) \emph{(synthetic; negative result reported as-is)} \\
seed-noise injection \{10/20/30\%\} & C5 & Purity 0.5339 $\to$ 0.5267 $\to$ 0.5201 $\to$ 0.5025 at 30\% ($-3.1$\,pp, still $1.52\times$ baseline)---iteration does not amplify seed noise at this scale \emph{(public-real / P0.3)} \\
\hline
\end{tabularx}
\end{table}

\paragraph{Pretraining value is a function of annotation scarcity.}
The pretraining row deserves its own reading because it quantifies the word \emph{low-resource} in our title. Across four matched budgets (88/176/352/1760 training clips, scratch vs.\ pretraining-initialized encoders, three seeds each, identical schedules), the fine-tuning benefit of the self-supervised encoder decays monotonically. It is $+21.2$\,pp at 88 clips---where training from scratch collapses to roughly $2.3\times$ the random baseline while the warm encoder reaches $7\times$ random---then $+9.9$\,pp at 176 clips (with warm-start variance $3.8\times$ narrower, a reliability gain independent of the mean), $-2.3$\,pp at 352 clips (inside the theoretical crossover region, smaller than both arms' standard deviations and disclosed as such), and $+0.15$\,pp (n.s.) at the saturated 1760-clip tier. The zero crossing lies between 10 and 20 clips per class; we bracket it rather than fabricate a precise location, and we pair the low-resource tiers with the saturated tier in every presentation to forestall cherry-picking. Together with the $k$-NN probe of Section~\ref{sec:main-results} (E1), this establishes two complementary lines of evidence for C3: representation discriminability (probe) and fine-tuning value (gradient). The mechanism reading is one sentence long: the scarcer the labels, the less a discriminative representation can be learned from data, and the cross-domain prior embedded by the frozen physics encoder becomes the only usable structure.

\paragraph{Semantic warm-start versus generic SSL on real skeletons.}
The synthetic-tier row above asks whether pretraining helps at all; on the public-real tier we ask the sharper design question of \emph{which} pretraining earns the low-label budget. Holding the entire end-to-end pipeline fixed (seeds, anchor-guided pseudo-labeling, head, splits, leakage guards) and replacing the backbone initialization together with its bound native input preprocessing (each arm uses the preprocessing its method was trained with; disclosed)---task-pretrained semantic warm start versus a generic contrastive encoder pretrained on motion-capture dog skeletons (AimCLR)---top-1 favors the warm start at every budget: $+5.9$\,pp at 2 clips/class (all ten same-seed pairs positive, Wilcoxon $p=0.002$), $+7.3$\,pp at 4 ($p=0.016$), and $+3.6$\,pp at full supervision; at 2 clips/class both arms beat the scratch control (no scratch arm was run at larger budgets). Macro-F1 is at parity at the scarce budget (14.36 vs.\ 14.78, $p=0.77$); an apparent generic-SSL lead in the initial three-seed run dissolved under the ten-seed expansion and is reported here as resolved seed noise rather than a trade-off. We disclose that the generic arm carries a mocap-to-in-the-wild domain gap that its original evaluation did not, so the arm is a transfer lower bound, not a best-tuned AimCLR. The reading this supports is narrow but design-relevant: at small budgets, what matters is not SSL pretraining per se but pretraining whose feature geometry is aligned to the target label structure---a reading consistent with, though not directly measured as, geometric alignment (no CKA-style probe was run).

\paragraph{Uncertainty sampling: an exploratory negative finding (C7, downgraded by pre-registration).}
We report, as an exploratory finding, that uncertainty sampling did not help in our regime. Under cold-start weak scorers on the synthetic tier, entropy sampling never outperformed random selection: random led by 7.9\,pp at budget 100 and 7.1\,pp at 200, with all three seeds agreeing. A full-budget rerun (120 vs.\ 50 epochs) reproduces the direction (12.3/7.8\,pp), and with warm-started in-domain fine-tuned scorers random still led by 4.2--5.0\,pp at every budget. A diagnostic explains the mechanism: the synthetic-trained checkpoint scored the public-real candidate pool with fully saturated softmax (mean top1--top2 logit margin 100.9, versus $\approx$10.8 within its own validation domain), reducing uncertainty signals to numerical zero. The classical preconditions of uncertainty sampling---a strong scorer and in-domain calibration---are exactly what a low-resource pipeline lacks at startup. This motivates a progressive annotation narrative (annotate first, calibrate the scorer, then select samples), which we leave to future work; we make no sampling-efficiency claims.

\paragraph{Sensitivity.}
Seed-ratio sweeps show prototype quality saturating early ($\leq$1\,pp beyond 25\% seeds); the confidence threshold $\tau$ and cluster count $K$ interact with the frequency-aware margin design, and full sweeps are registered as future work. The class-imbalance handling switch (frequency-aware margins vs.\ resampling) is evaluated on the public-real tier together with the per-class breakdown mandated by Section~\ref{sec:conclusion}.

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{fig4_al_efficiency.pdf}
  \caption{Active-learning efficiency on the synthetic layer (short-budget protocol). Best validation accuracy (22-class top-1 on a fixed ground-truth validation set, $n=330$; mean $\pm$ std over 3 seeds) versus annotation budget, with cold-start retraining at every budget point. Both arms saturate far above the 4.5\% random-guess baseline (22 classes). Under this cold-start protocol, uncertainty sampling (softmax entropy) shows no efficiency advantage over random selection: it does not outperform random at any budget and is exceeded by random for budgets $\geq 100$ (e.g., 69.9\% vs.\ 77.8\% at $b=100$; 80.9\% vs.\ 88.0\% at $b=200$).}\label{fig:al-efficiency}
\end{figure}
